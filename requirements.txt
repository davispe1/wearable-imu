# gait-opensim runtime dependencies.
#
# NOTE: opensim is intentionally NOT listed. OpenSim/OpenSense runs as a separate GUI
# application and consumes the .sto files this package writes; it is never imported here.

numpy>=1.20
scipy>=1.7          # signal/quaternion utilities (gait-event peak finding, convention check)
vqf>=2.1            # Laidig & Seel (2023) validated orientation filter
matplotlib>=3.5     # app.viewer — joint-angle / gait-cycle / parameter figures
