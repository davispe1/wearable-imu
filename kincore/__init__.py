"""kincore — self-contained kinematic core.

Consumes BIN-native IMU only (accel + gyro + magnetometer). Never sees marker-derived
reference angles or gait-event labels (those live behind the validation wall).
"""
